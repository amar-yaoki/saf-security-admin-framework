<?php
/**
 * version.php — SAF version constants.
 * Caricato sia da saf.php (plugin principale) che da saf-loader.php (MU).
 */

defined( 'ABSPATH' ) || exit;

define( 'SAF_VERSION', '2.0.0' );
define( 'SAF_DIR', plugin_dir_path( __FILE__ ) );
define( 'SAF_URL', plugin_dir_url( __FILE__ ) );
